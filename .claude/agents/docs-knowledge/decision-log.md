# 決定ログ

| 日付 | 決定事項 | 未解決 | 担当 |
|------|---------|--------|------|
| 2026-03-28 | ロケーション詳細の時間帯タイムラインは **短ラベルのみ**（例 `18–24` / `0–6`）。**「前日18–24」「当日0–6」等の複合ラベルは禁止**し日跨ぎ文脈は **上辺帯色＋レジェンド／L2／ツールチップ** に寄せる。スロットは **折返し禁止・省略（ellipsis）・最大2行相当の視覚階層**、**アイコン主・数値副**、**横スクロール**。ターゲット **A＝比較・密度（L2＋8スロット）** / **B＝1秒判断（glance 帯＋タイムライン帯の吉凶）** を切替可能にする設計。カレントモック **`.claude/agents/product-ux/mock-011.html`**。 | `qa-accessibility/test-checklist.md`・`regression-list.md`、`architecture/touch-points.md` はプレースホルダのまま。**実装前**に qa が合格条件を追記し、architecture が `ski-powder-hunter*.html` の当たりを `touch-points.md` に記載すること。 | docs-knowledge（集約） |
| 2026-03-28 | **ゲレンデフォーカスビュー・タスク**（画面状態案 `FocusView_FromRanking`）：トップランキング等の一覧から一発フォーカス後の **正 UI は mock-013**（薄マップ帯＋シート主体、L1/L2/L3、タイトル横の小「次 →」）。**JA 視覚の参照基準**は **`.claude/agents/product-ux/mock-archive/mock-011.html`**。**英語**は **mock-013** の `lang="en"` 制度（`--slot-w`、2 行ラベル、雨バッジ折返し、`labelShort` / `labelAria`）。**マップピン経路の大型シート（mock-012）とはテンプレ混在禁止**（同一 `contentHtml` / DOM に両系統を足さない）。詳細定義 **`.claude/agents/product-ux/gelande-focus-view-task.md`**、集約 Handoff **`.claude/agents/docs-knowledge/handoffs/gelande-focus-view-task/HANDOFF.md`**。 | **P0** ゴー／リスクの算法と文言（snow-data 連携）。**P1** B/A の永続・自動判定の要否。**P2** 薄マップ帯からのマップ全画面導線。**設計未決** 薄マップ＋シートのルート（`#view-map` 内新ラッパ vs 既存 `#map-detail-sheet` の経路別差し替え）。マップ経路 `FocusView_FromMap` は **mock-012 系は別案**、実装時に分岐方針確定。 | product-ux 起点 → implementer（ゲート後）／snow-data／qa-accessibility |
| 2026-03-28 | **マップビュー補正タスク**：**構造・禁止 UI・MVC 差分のマスター**は **`.claude/agents/docs-knowledge/handoffs/map-view-correction-task/HANDOFF.md`**（正本 DOM は `#device` / `.device-screen` 相当のブロック順、B/A は `.b-glance` / `.l2` の `hidden` のみ、注釈列・「ひと目／詳細」・grab 等は本番禁止）。**見た目・タッチ・補助色の最終比較対象**は **`.claude/agents/product-ux/mock-014.html` の `.device-screen` 内**（mock-013 土台＋ **44px ターゲット**、**`--muted-caption: #b8c8dc`**、補助文のコントラスト改善）。HANDOFF 内「最終 arbit は mock-013」は実務上 **mock-014 のデバイス内**に読み替え。 | **MVC-17** 暦日は **暦日入り次連番モック採用後**にのみ本番一致を要求（現段階では mock と同様の相対日付のまま暦日を Handoff 判断で追加しない）。本番トークンでの **`--muted-caption` コントラスト実測**。タスク定義 **`.claude/agents/product-ux/map-view-correction-task.md`**（存在すれば同期）。 | product-ux／architecture → implementer（ゲート後） |
| 2026-06-22 | **docs-knowledge フェーズ 1（gelande-focus-view-task）**: ドキュメント正本を **mock-016** に統一。**MVC-06** を日付チップ **3 段**（相対 `span` ＋ `.day-chip__cal` ＋ `strong`）に再定義し、旧 **MVC-17**（暦日専用）を **MVC-06 に吸収**。更新: `screen-flow.md`、`map-view-correction-task/HANDOFF.md`、`map-view-correction-task.md`、本タスク `HANDOFF.md`。 | **P0** qa-accessibility による mock-016 再評価（フェーズ 4）。本番移植は QA 合格後。 | docs-knowledge |
