# [HANDOFF] 2026-03-28 — from: snow-data

## 会話の要約

- ユーザーは **`@snow-data`** を指定し、**Handoff ルール**を明示した。
- ルール内容：**会話の最後に必ず** (1) `[HANDOFF]` メモを生成する (2) `.claude/agents/docs-knowledge/handoffs/[日付]-[from].md` に保存する。
- 本ターンでは上記に従い、`from: snow-data`・日付 **2026-03-28** として handoff ファイルを新規作成した。

## [HANDOFF] ブロック（エージェント定義との対応）

`snow-data/agent.md` 内テンプレート:

```text
[HANDOFF]
from: snow-data
決定事項:
誤解リスク:
実装エージェントへの注意点:
```

| 項目 | 内容 |
|------|------|
| **決定事項** | Handoff の保存先を **`docs-knowledge/handoffs/YYYY-MM-DD-snow-data.md`** とする運用をユーザーが指示した（本ファイルがその実体）。 |
| **誤解リスク** | `agent.md` には別途「`decision-log.md` に追記を依頼」とある。**ユーザー指示が handoffs パスを優先**する場合は、本パスを正とするか、両方の同期方針を決める必要がある。 |
| **実装エージェントへの注意点** | `snow-data` は **`can_edit_code: false`**。本番実装パス（`ski-powder-hunter*.html`、`scripts/`、`package.json` 系）には**ツールで触れない**。データ定義・マッピング・境界テストは `.claude/agents/snow-data/` 配下の Markdown を参照。 |

## 成果物（パス・参照）

| 内容 | パス |
|------|------|
| snow-data エージェント定義 | `.claude/agents/snow-data/agent.md` |
| データ辞書 / マッピング / 境界テスト | `data-dictionary.md`, `resort-mapping.md`, `boundary-tests.md`（同一ディレクトリ） |
| 本 handoff | `.claude/agents/docs-knowledge/handoffs/2026-03-28-snow-data.md` |

## 次セッション向け

1. **decision-log.md との関係**を揃えるなら、`docs-knowledge/decision-log.md` への追記要否をユーザーまたは docs-knowledge 担当で決める。
2. 雪・気象の**具体タスク**が続く場合は、上記 `[HANDOFF]` ブロックを会話内容で埋め、同様に `handoffs/日付-snow-data.md` を更新する。

## メタ

- 保存日: 2026-03-28  
- `from`: `snow-data`
