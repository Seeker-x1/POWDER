# 指示 A1 — ペンディング（実装は未着手）

**状態**: ペンディング（エージェント階層での Handoff → 「実装して」まで待ち）

## 概要

全ペルソナ共通の課題として、**モバイルのトップビューでゲレンデ検索・絞り込みができない**ため、「行きたいゲレンデに辿り着けない」不便が発生している。トップで検索（または同等の候補絞り込み）できるようにする。

## 背景（ユーザー洞察）

- トップで目的のゲレンデに絞れないことが、不便感の要因になっている。
- ペルソナ（ユウ／ミナ ほか）横断で有効な改善。

## 次のステップ（階層運用）

1. **docs-knowledge**: `docs/product/personas.md` と整合するよう受け入れ条件・`IMPLEMENTATION_ORDER_*` 案を整形し、Handoff を生成。
2. **implementer**: Handoff 受領後、担当者が会話（または handoff ファイル）末尾に **「実装して」** を付与したうえで実装着手。

## 参照

- `docs/product/personas.md`
- `docs/implementation/CURRENT_SOURCE_OF_TRUTH.md`
